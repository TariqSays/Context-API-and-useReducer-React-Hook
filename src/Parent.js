import React from 'react';
import Child from './Child';

function Parent(){
    return(
        <div>
            Parent Component
            <Child></Child>
        </div>
    );
}
export default Parent;